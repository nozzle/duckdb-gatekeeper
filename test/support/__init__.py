"""Importable helpers for the Python suite. Fixtures live in test/conftest.py; import functions and constants
from the modules here explicitly (``from support.typed_helpers import validate``), never from conftest."""
